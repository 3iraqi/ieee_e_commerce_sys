// import 'Category.dart';

class Product {

     int ?    productId ;
     String ? productName ;
     String ? description ;
     double ? productPrice ;
     String ? supplierName ;
     int ?    availableQuantity ;


     Product({
       this.productId,
       this.productName,
       this.description,
       this.productPrice,
       this.supplierName,
       this.availableQuantity,

     }
     );



}