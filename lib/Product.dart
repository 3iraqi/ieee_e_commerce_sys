class Product{

     int ?    productId ;
     String ? productName ;
     String ? description ;
     double ? productPrice ;
     String ? supplierName ;
     int ?    availableQuantity ;
     String? categoryId;

     Product({
       this.productId,
       this.productName,
       this.description,
       this.productPrice,
       this.supplierName,
       this.availableQuantity
     }
     );

}