class Category{

  String ? categoryName;
  String ? categoryId;
  String ? color;
  String ? brand;

  Category(
    {
      this.categoryName,
      this.categoryId,
      this.color,
      this.brand,

    }
  );

}